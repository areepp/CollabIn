---
import { slide } from 'astro:transitions'

interface Props {
  className?: string
}
const { className } = Astro.props
---

<main transition:animate={slide({ duration: '1s' })} class={className}>
  <slot />
</main>
